datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Product {
  id          String  @id
  name        String
  description String
  price       Float
  category    String
  sku         String
  imageUrl    String?
}
