// import { Context } from "../../types/context";

// export const customerResolvers = {
//   Query: {
//     customers: async (_: any, __: any, context: Context) => {
//       return await context.services.customerService.getAllCustomers();
//     },

//     customer: async (_: any, { id }: { id: string }, context: Context) => {
//       return await context.services.customerService.getCustomerById(id);
//     },
//   },
// };
